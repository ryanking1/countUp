describe("countUp", function() {
  it("sets input value to count to", function() {
    expect(endCount).to.equal(50);
  });
  it("sets incrementor value to count by", function(){
    expect(incrementor).to.equal(5);
  });
});
